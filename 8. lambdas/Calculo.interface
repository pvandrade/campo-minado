package lambdas



@FunctionalInterface
public interface Calculo {
    
    //definição de método em interface é, por padrão, public e abstract.
    //não precisa conter as palavras public e abstract.
    //Interfaces funcionais podem conter apenas um método abstract.
    // mas podem conter metodos default, static, etc.
    //A expressão lambda será a implementação do método abstract.
    double executar (double a, double b);
}
